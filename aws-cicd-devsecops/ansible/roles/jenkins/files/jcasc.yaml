jenkins:
  systemMessage: "Catalogix CI/CD — configured automatically via JCasC"
  numExecutors: 2
  mode: NORMAL

  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: "${JENKINS_ADMIN_USER}"
          password: "${JENKINS_ADMIN_PASSWORD}"
    
  authorizationStrategy:
    globalMatrix:
      permissions:
        - "Overall/Administer:${JENKINS_ADMIN_USER}"

unclassified:
  location:
    url: "http://${JENKINS_URL}:8080/"

  sonarGlobalConfiguration:
    installations:
      - name: "sonarqube"
        serverUrl: "http://${SONARQUBE_HOST}:9000"
        credentialsId: "sonarqube-token"

tool:
  git:
    installations:
      - name: "git"
        home: "/usr/bin/git"

  maven:
    installations:
      - name: "maven"
        properties:
          - installSource:
              installers:
                - maven:
                    id: "3.9.9"
 
  nodejs:
    installations:
      - name: "node"
        properties:
          - installSource:
              installers:
                - nodeJSInstaller:
                    id: "20.18.0"
                    npmPackagesRefreshHours: 72

credentials:
  system:
    domainCredentials:
      - credentials:
          # GitHub Token
          - string:
              scope: GLOBAL
              id: "github-token"
              secret: "${GITHUB_TOKEN}"
          # SonarQube Token
          - string:
              scope: GLOBAL
              id: "sonarqube-token"
              secret: "${SONARQUBE_TOKEN}"
          # AWS IAM user credentials — access key + secret key
          # Used by all AWS CLI and Terraform calls in the pipelines
          - aws:
              scope: GLOBAL
              id: "aws-creds"
              description: "AWS IAM user credentials for Catalogix"
              accessKey: "${AWS_ACCESS_KEY_ID}"
              secretKey: "${AWS_SECRET_ACCESS_KEY}"

# Job Definitions — both pipelines created automatically on Jenkins startup
jobs:
  # Platform Infra Pipeline
  - script: >
      pipelineJob('platform-infra') {
        description('Provision infrastructure using Terraform')
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('${GIT_REPO_URL}')
                  credentials('github-token')
                }
                branches('*/main')
              }
            }
            scriptPath('Jenkinsfile.platform-infra')
          }
        }
      }

  # Application CICD Pipeline
  - script: >
      pipelineJob('app-cicd') {
        description('Application CICD Pipeline - Build, Scan, Deploy')
        triggers {
          githubPush()
        }
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('${GIT_REPO_URL}')
                  credentials('github-token')
                }
                branches('*/main')
              }
            }
            scriptPath('Jenkinsfile.app-cicd')
          }
        }
      }