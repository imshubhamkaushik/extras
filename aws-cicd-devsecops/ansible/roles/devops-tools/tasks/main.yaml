---
# Tasks for installing DevOps tools like AWS CLI, kubectl, Helm, and Trivy
# Tool versions are defined in ansible/group_vars/all.yaml for single-source control and easy maintenance. 
# Ensure that the EC2 instances have access to the internet to download these tools

# AWS CLI
- name: Check AWS CLI installation
  command: aws --version
  register: aws_cli_check
  changed_when: false
  failed_when: false

- name: Download AWS CLI installer
  get_url:
    url: https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip
    dest: /tmp/awscliv2.zip
    mode: '0644'
  when: aws_cli_check.rc != 0

- name: Unzip AWS CLI installer
  unarchive:
    src: /tmp/awscliv2.zip
    dest: /tmp
    remote_src: yes
  when: aws_cli_check.rc != 0

- name: Install AWS CLI
  command: /tmp/aws/install
  when: aws_cli_check.rc != 0

# Kubernetes CLI (kubectl)
- name: Check kubectl version
  command: kubectl version --client --output=json
  register: kubectl_check
  changed_when: false
  failed_when: false

- name: Install kubectl {{ kubectl_version }}
  get_url:
    url: https://dl.k8s.io/release/{{ kubectl_version }}/bin/linux/amd64/kubectl
    dest: /usr/local/bin/kubectl
    mode: '0755'
    force: true
  when: kubectl_version not in (kubectl_check.stdout | default(''))

# Helm
- name: Check Helm version
  command: helm version --short
  register: helm_check
  changed_when: false
  failed_when: false

- name: Download Helm
  get_url:
    url: https://get.helm.sh/helm-{{ helm_version }}-linux-amd64.tar.gz
    dest: /tmp/helm.tar.gz
    force: true
  when: helm_version not in (helm_check.stdout | default(''))

- name: Extract Helm Archive
  unarchive:
    src: /tmp/helm.tar.gz
    dest: /tmp
    remote_src: yes
  when: helm_version not in (helm_check.stdout | default(''))

- name: Install Helm binary
  copy:
    src: /tmp/linux-amd64/helm
    dest: /usr/local/bin/helm
    mode: '0755'
    remote_src: yes
  when: helm_version not in (helm_check.stdout | default(''))

# Trivy
- name: Check Trivy version
  command: trivy --version
  register: trivy_check
  changed_when: false
  failed_when: false

- name: Download Trivy {{ trivy_version }} .deb package
  get_url:
    url: https://github.com/aquasecurity/trivy/releases/download/v{{ trivy_version }}/trivy_{{ trivy_version }}_Linux-64bit.deb
    dest: /tmp/trivy.deb
    force: true
  when: trivy_version not in (trivy_check.stdout | default(''))

- name: Install Trivy from .deb package
  apt:
    deb: /tmp/trivy.deb
  when: trivy_version not in (trivy_check.stdout | default(''))

# Sonar Scanner
- name: Check Sonar Scanner version
  command: sonar-scanner --version
  register: sonar_check
  changed_when: false
  failed_when: false

- name: Download Sonar Scanner {{ sonar_scanner_version }}
  get_url:
    url: "https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-{{ sonar_scanner_version }}-linux-x64.zip"
    dest: /tmp/sonar-scanner.zip
    force: true
  when: sonar_scanner_version not in (sonar_check.stdout | default(''))

- name: Unzip Sonar Scanner
  unarchive:
    src: /tmp/sonar-scanner.zip
    dest: /opt/
    remote_src: yes
  when: sonar_scanner_version not in (sonar_check.stdout | default(''))

# Create symlink so 'sonar-scanner' is available on PATH without full path.
# Previously the binary was extracted to /opt/ but never added to PATH.
- name: Create sonar-scanner symlink in /usr/local/bin
  file:
    src: "/opt/sonar-scanner-cli-{{ sonar_scanner_version }}-linux-x64/bin/sonar-scanner"
    dest: /usr/local/bin/sonar-scanner
    state: link
    force: yes
  when: sonar_scanner_version not in (sonar_check.stdout | default(''))